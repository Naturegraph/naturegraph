/**
 * Guards — Composants de protection des routes
 *
 * ProtectedRoute : pages nécessitant une authentification
 * PublicRoute : pages accessibles uniquement aux visiteurs non connectés
 */

export { ProtectedRoute } from './ProtectedRoute'
export { PublicRoute } from './PublicRoute'
