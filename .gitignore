# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local
# Env files : couvrir TOUTES les variantes (BATCH 39 hardening)
.env
.env.*
!.env.example

# Design references & mockups (not versioned)
design-references/
*.old.tsx

# Figma Make generated source (reference only, not part of the app)
naturegraph-make/

# Supabase CLI temporary cache (gen types, etc.)
supabase/.temp/

# Claude Code config (machine-specific) : ignore tout SAUF les skills partages
# (process d'equipe versionnes, ex. release-prod). settings.local.json, launch.json
# et worktrees restent locaux.
.claude/*
!.claude/skills/

# Donnees privees / PII (vraies adresses email, listes de contacts) : NE JAMAIS
# versionner dans un repo public (fuite RGPD). Conserve en local uniquement.
private/

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?
Taxref/

# Playwright (BATCH 20 / T-009)
/test-results/
/playwright-report/
/playwright/.cache/

# Vitest coverage (BATCH 23 / T-008)
/coverage/
scripts/.taxonomy-seed-cache/

# Artefact genere par scripts/run-dev-rebuild.mjs (dump dev, regenerable, ~360K)
# Ne pas versionner : il se regenere lors de la reprise du rebuild dev (NG-007).
scripts/dev-rebuild.sql

# NG-007 : gros SQL generes localement (rebuild dev), non versionnes
scripts/dev-rebuild.sql
scripts/dev-rebuild-full.sql
scripts/chunks/
supabase/prod_schema.sql
