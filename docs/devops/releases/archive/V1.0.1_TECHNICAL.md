# Release V1.0.1, note technique

> Archive technique interne. NE PAS communiquer aux users (release admin only).

**Date de deploiement** : 2026-05-25
**Type** : PATCH (admin only, zero impact user)
**Tag git** : `v1.0.1`
**Decideur** : Nicolas

---

## Resume

Premiere PATCH post V1.0.0. Fix mineur admin uniquement.

---

## Changements

### Admin uniquement

- **AdminBeta, motivation waitlist visible en entier** :
  - Avant : `line-clamp-1` + `max-w-xs` tronquait le message a 1 ligne dans la table waitlist
  - Apres : retire le clamp + `max-w-md` + `whitespace-pre-line`
  - Impact : Nicolas voit l integralite du message de motivation laisse par chaque inscrit waitlist directement dans le tableau

### Securite (impact user marginal)

- **Route /post/:postId protegee** :
  - Avant : OnboardingGuard seul, accessible aux non-authentifies (qui ont juste la beta key)
  - Apres : ProtectedRoute wrapper, redirect vers /login si non authentifie (puis vers /welcome si pas de beta key non plus)
  - Cas : page de detail d un post n est plus accessible en lecture libre, exige une session valide

### Operationnel (deja fait hors release, en SQL direct)

- **Code beta NG-HMW8-D9U6 lie a Claire** :
  - UPDATE beta_access_keys + INSERT beta_signup_log pour tracabilite
  - Bug a creuser : le claim atomique n a pas tourne lors du signup de Claire
  - A auditer dans V1.0.2 ou V1.1.0

---

## Actions techniques

- [x] Force-logout requis ? **NON** (changement admin uniquement)
- [x] Migration SQL ? **NON**
- [x] Hard refresh ? Recommande pour Nicolas (cache navigateur admin)
- [x] Tag git : `git tag -a v1.0.1 -m "Release v1.0.1 admin fix"`
- [x] PR develop > main : squash admin

---

## Risques

Aucun. Changement isole a `src/pages/Admin/AdminBeta.tsx`, n affecte aucun user public.

---

## Rollback

```bash
git revert <merge-commit-hash>
git push origin main
```

---

## Validation Nicolas

- [x] Validation directe par Nicolas : "tu peux tout push en prod pas de soucis, cela concerne l'admin"
- [x] Pas de note user-friendly (release admin only)
