# Release V0.0.1 (technique)

> Date : 2026-06-17 · Type : base officielle (renumerotation) · Tickets : NG-025, NG-024

---

## Resume

Premiere version officielle du track de prelancement public. **Renumerotation
volontaire** : la version passe de `1.2.25` (numerotation interne de la phase de
construction) a `0.0.1` (MVP valide, base propre). Aucun changement fonctionnel,
aucune migration, aucun changement de schema. Relabel + documentation uniquement.

## Contenu

- `chore(versioning)` (cb96fd4) : adoption SemVer + Conventional Commits (NG-025).
  - `package.json` : `1.2.25` -> `0.0.1` (NG-024). Affichage footer reglages mis a jour.
  - `docs/devops/VERSIONING.md` : norme complete (SemVer, commits, tags, Releases, suivi Notion, jalons V0.0.x -> V1.0.0).
  - `docs/devops/RELEASE_PROCESS.md` : reconcilie (renvois vers VERSIONING, plus de contradiction V1.x).
  - fix : em-dash retire de la description `package.json`.
- `docs(devops)` (94f0367) : kickoff phase prelancement (PRELANCEMENT_KICKOFF.md).

## Risques

- Faible. La version affichee dans l'app passe de `1.2.25` a `0.0.1` (cosmetique).
- Les 43 tags `v1.x` restent dans l'historique git (trace du churn de dev). Le nouveau
  track public demarre a `v0.0.1`. Pas de collision (noms differents).

## Maintenance utilisateur

- Force-logout : NON.
- Notification in-app : NON (relabel silencieux, pas de changement visible fonctionnel).

## Rollback

- Code : `git revert cb96fd4` (rétablit `1.2.25`). Aucun effet de bord (relabel pur).
- DB : sans objet (aucune migration).

## Validation

- [x] Build / lint OK (lint-staged au commit, JSON valide verifie).
- [x] Flux develop -> staging -> main (ff).
- [x] Tag `v0.0.1` pose sur `main`.
- [x] Autorisation fondateur (2026-06-17).
