# V0.7.19 : ROLLBACK d'urgence (black screen mobile) -> etat 0.7.15

## Contexte

Retour Nicolas (urgence) : black screen sur mobile apres le splash (post-login),
persistant meme apres refresh. Blocage utilisateurs potentiel.

Pour stabiliser sans perdre de temps, on revient a l'etat STABLE 0.7.15 sur les
fichiers touches par la "saga" splash/anti-fantome (0.7.16-0.7.18) :

- `src/App.tsx` (BootSplash) : retour 0.7.15 (le gate mobile est retire ; le residu
  desktop revient temporairement, on le retraitera proprement).
- `src/components/ui/AppLoader.tsx` : retour 0.7.15 (video restauree).
- `src/hooks/useContributePostSubmit.ts`, `src/services/postService.ts` : retour
  0.7.15 (le client anti-fantome pending->flip est retire).

Conserve : tout le reste (va-et-vient, recherche resiliente, toasts, retry 401,
fix E2 email edge v12). La migration DB anti-fantome reste appliquee mais devient
INERTE (le client ne cree plus de 'pending') : retro-compatible, aucun impact.

## Diagnostic en cours

Verification faite : la Landing rend correctement sur mobile 0.7.18, console sans
erreur JS, et les changements 0.7.16-0.7.18 ne touchent pas le rendu du /home
connecte. Le black screen pointe donc plutot vers l'etat de session/auth cote
/home connecte. Ce rollback stabilise ; la cause reelle est a diagnostiquer avec
la console mobile ou un event Sentry.

## Tests

- `npm run build` : OK
- `npm test` : 148/148 OK
