# Release V0.7.7 : va-et-vient - detection de gel abaissee + confirmation Sentry

> Base V0.7.6. Aucune migration. Renforce la reprise du "bouton mort au va-et-vient".

- Seuil de detection de gel abaisse (8s -> 6s) : capte des retours d'arriere-plan
  plus courts sur mobile.
- CONFIRMATION Sentry : avant un reload-de-gel on pose un marqueur ; au boot suivant
  on remonte un EVENEMENT (trackFailure "app.resume / reload-after-freeze", visible
  dans la liste des issues, contrairement aux breadcrumbs). Permet de VERIFIER que la
  reprise s'est declenchee, meme quand la page gelee n'avait rien pu envoyer.
- Rappel : la detection repose sur le battement de coeur (setInterval gele en
  arriere-plan), independant de visibilitychange/pageshow (peu fiables en PWA). Au
  degel, le 1er tick voit l'ecart -> reload. Brouillon + photos restaurent la saisie.

Test (PWA mobile, Rencontre ET Instant) : completer, arriere-plan ~10s, revenir ->
l'app doit se RECHARGER (le panneau se ferme, on revient au feed) -> rouvrir le
panneau (saisie restauree) -> Publier marche. Confirmation : un event Sentry
"reload-after-freeze" apparait apres chaque va-et-vient qui a recharge.

Verifie : build OK, eslint 0 erreur, 148 tests OK.
