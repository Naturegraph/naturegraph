# Release V0.1.1 : panneau Parametres Notifications fonctionnel (technique)

> Date : 2026-07-09 · Tag : `v0.1.1` · Branche : `main` (prod)
> Precedent : `v0.1.0`
> Force-logout : NON · Migrations : 0 (deja appliquees) · Notif in-app : non
> Ticket : NG-045 / NG-046

PATCH critique : rend le panneau Parametres > Notifications reellement
fonctionnel (il etait encore en mode "Bientot" alors que les emails NG-045
partent deja en prod). Les utilisateurs peuvent enfin gerer leurs preferences
depuis l'app, pas seulement via le lien de desabonnement des emails.

## Contenu

- **Panneau Notifications fonctionnel** (SettingsNotificationsView) : flag
  `SOON_FEATURE` retire. Chaque section ecrit dans la vraie source et est
  respectee par le backend :
  - "Quelles notifications recevoir" -> notification_preferences.enabled +
    email_enabled (les 2 canaux).
  - "Methodes" -> user_settings.email_notifications (master email).
  - "Nouvelles et mises a jour" -> user_settings.newsletter.
  - "Frequence" -> user_settings.notif_frequency.
  - Service + hook etendus au canal email (setPreferenceChannels, isEmailEnabled).
- **Arrondi de la page /notifications** reduit (rounded-xl 32px -> rounded-lg
  20px), coherent avec le panneau cloche (NG-046).
- Migration de tracabilite du durcissement des grants is_email_enabled.

## Verifications

- `npm run build` OK (tsc -b + vite). Round-trip DB verifie : couper un type
  coupe la cloche ET l'email (is_notif_enabled + is_email_enabled false).

## Note

- Deux casts temporaires (`touch_last_active`, payload `email_enabled`) en
  attendant la regeneration de `src/types/supabase.ts` (tache de durcissement
  en cours).
